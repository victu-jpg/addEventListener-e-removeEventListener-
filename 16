let contador = 0;

let botao = document.getElementById("botao");
let texto = document.getElementById("texto");

function contar(){

    if(contador < 10){

        contador++;

        texto.textContent = contador;

    }else{

        texto.textContent = "Limite atingido";

        botao.removeEventListener("click",contar);

    }

}

document.getElementById("ativar").addEventListener("click",function(){

    botao.addEventListener("click",contar);

});


let contador = 0
let botao = document.getElementById("botao")
let texto = document.getElementById("texto")
function contar(){
    if(contador < 10){
        contador++
        texto.textContent = contador
    }else{
        texto.textContent = "Limite atingido"
        botao.removeEventListener("click",contar)
    }
}
document.getElementById("ativar").addEventListener("click",function(){
    botao.addEventListener("click",contar)
})
